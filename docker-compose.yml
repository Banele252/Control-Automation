#version: "3.8"

services:
  backend:
    build: ./BackEnd
    ports:
      - "8000:8000"
    env_file:
      - .env
  frontend:
    depends_on:
      - backend
    build: ./FrontEnd/gradio
    ports:
      - "7861:7861"
    env_file:
      - .env